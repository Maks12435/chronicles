# Chronicles - Демо-режим

Это демо-версия приложения Chronicles, которая работает без Docker и PostgreSQL. Просто склонируйте проект и запустите одной командой!

## 🚀 Быстрый запуск

### 1. Клонирование проекта
```bash
git clone https://github.com/Maks12435/MAXISM.git
cd MAXISM
```

### 2. Установка зависимостей

#### Backend (Python)
```bash
cd backend
pip install -r requirements.txt
```

#### Frontend (React)
```bash
cd ../react
npm install
```

### 3. Запуск приложения

#### Запуск бэкенда (в одном терминале)
```bash
cd backend
python demo_main.py
```
Бэкенд будет доступен по адресу: `http://localhost:8080`

#### Запуск фронтенда (в другом терминале)
```bash
cd react
npm run dev
```
Фронтенд будет доступен по адресу: `http://localhost:5173`

## 🔑 Демо-аккаунт

Для входа используйте:
- **Email**: `demo@chronicles.com`
- **Пароль**: `demo1234`

## 📋 Функциональность

Приложение включает следующие модули:

### 🎵 Музыка
- Просмотр демо-треков
- Статистика прослушиваний
- Рейтинги исполнителей

### 🎬 Фильмы и сериалы
- Каталог фильмов и сериалов
- Персональные рейтинги
- История просмотров

### 📚 Книги
- Библиотека книг
- Рейтинги и отзывы
- Чтение онлайн

### ✅ Задачи
- Создание и управление задачами
- Отслеживание прогресса
- Категории и приоритеты

### 📝 Дневник
- Личные записи
- Категории записей
- Поиск и фильтрация

## 🗃️ Демо-данные

При первом запуске автоматически создается SQLite база данных с демо-данными:

- **3 музыкальных трека** (The Weeknd, Dua Lipa)
- **3 фильма** (Inception, The Dark Knight, Interstellar)
- **2 сериала** (Breaking Bad, Stranger Things)
- **2 книги** (1984, To Kill a Mockingbird)
- **3 задачи** с разными статусами
- **3 записи дневника** разных типов

## 🔧 Технические особенности

- **Backend**: FastAPI + SQLite
- **Frontend**: React + TypeScript + Vite
- **Аутентификация**: Демо-режим с фиксированным пользователем
- **База данных**: SQLite (файловая, не требует установки сервера)
- **CORS**: Настроен для локальной разработки

## 🌐 API Endpoints

### Аутентификация
- `POST /auth/login` - Вход в систему
- `POST /auth/register` - Регистрация
- `GET /auth/me` - Информация о пользователе
- `GET /auth/check` - Проверка авторизации
- `POST /auth/logout` - Выход из системы

### Музыка
- `POST /music/music_search` - Поиск треков
- `POST /music/artist_search` - Поиск исполнителей
- `GET /music/get_tracks` - Получить пользовательские треки
- `POST /music/add_track` - Добавить трек
- `POST /music/swap_track_rank` - Изменить рейтинг трека
- `POST /music/delete_track` - Удалить трек
- `GET /music/stats` - Статистика музыки

### Фильмы и сериалы
- `GET /shows/movie_search` - Поиск фильмов
- `GET /shows/get_movies` - Получить пользовательские фильмы
- `POST /shows/add_movie` - Добавить фильм
- `POST /shows/delete_movie` - Удалить фильм
- `GET /shows/series_search` - Поиск сериалов
- `GET /shows/get_series` - Получить пользовательские сериалы
- `POST /shows/add_series` - Добавить сериал
- `POST /shows/delete_series` - Удалить сериал
- `POST /shows/update_rating` - Обновить рейтинг
- `GET /shows/stats` - Статистика шоу

### Книги
- `GET /books/books_search` - Поиск книг
- `GET /books/get_books` - Получить пользовательские книги
- `POST /books/add_book` - Добавить книгу
- `POST /books/delete_book` - Удалить книгу
- `POST /books/update_rating` - Обновить рейтинг книги
- `GET /books/stats` - Статистика книг

### Задачи
- `GET /tasks/get_tasks` - Получить задачи
- `POST /tasks/add_task` - Добавить задачу
- `POST /tasks/delete_task` - Удалить задачу
- `POST /tasks/update_task` - Обновить задачу
- `GET /tasks/stats` - Статистика задач

### Дневник
- `GET /diary/get_diary_records` - Получить записи дневника
- `POST /diary/add_diary_record` - Добавить запись дневника
- `POST /diary/delete_diary_record` - Удалить запись дневника
- `POST /diary/update_diary_record` - Обновить запись дневника
- `GET /diary/stats` - Статистика дневника

## 🐛 Решение проблем

### Проблема: Ошибка порта 8080 уже используется
```bash
# Найдите процесс, использующий порт
netstat -ano | findstr :8080
# Завершите процесс или измените порт в demo_main.py
```

### Проблема: Ошибка зависимостей
```bash
# Переустановите зависимости
pip install --upgrade -r requirements.txt
npm install
```

### Проблема: CORS ошибки
Убедитесь, что фронтенд запущен на порту 5173, а бэкенд на порту 8080.

## 📁 Структура проекта

```
Chronicles/
├── backend/
│   ├── demo_main.py          # Демо-версия бэкенда
│   ├── demo_db.py            # Конфигурация SQLite
│   ├── demo_data.py          # Скрипт создания демо-данных
│   └── demo_routers/         # Демо-роутеры
├── react/                    # Фронтенд React
└── README_DEMO.md           # Эта инструкция
```

## 🎯 Что можно тестировать

1. **Авторизация** - вход с демо-учетными данными
2. **Добавление контента** - попробуйте добавить новый трек, фильм или книгу
3. **Обновление данных** - измените рейтинги, статусы задач
4. **Поиск и фильтрация** - проверьте работу фильтров в разных модулях
5. **Статистика** - просмотрите графики и отчеты

## ⚠️ Ограничения демо-режима

- Данные сохраняются только в текущей сессии
- При перезапуске сервера данные сбрасываются к исходным
- Некоторые функции API могут быть упрощены
- Отсутствует интеграция с внешними API (Spotify, TMDB)

---

**Примечание**: Это демо-версия для ознакомления с функциональностью. Для полной версии с PostgreSQL и всеми функциями используйте основную конфигурацию с Docker.
